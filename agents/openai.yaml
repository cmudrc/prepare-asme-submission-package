interface:
  display_name: "Prepare JMD Submission"
  short_description: "Build validated JMD/JCISE submission packages"
  default_prompt: "Use $prepare-jmd-submission to turn this Word document or Overleaf ZIP into a validated final-submission package."
